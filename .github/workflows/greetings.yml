name: Esteira simples

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Echo
        run: echo "Hello, world!"

      - name: Echo 2
        run: echo "Rodei a esteira"
